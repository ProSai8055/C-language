#include <stdio.h>
int main()
{
     int a, b, c;
    // a = b = 10;
    // c = a < 10;
    // printf("\nx=%d\ny=%d\nc=%d", a, b, c);
    // int k     = 35;
    // int s=35;
 
    // printf("\n%d %d %d", s==35, s = 50, s > 40);
    // int i=65;
    // char j='A';
    // if(i==j)
    // printf("\nWOow");
    // else
    // printf("Coow");
    if(3+2%5)
    printf("this works");
    if(a=10)
    printf("\nTHis also works");
    if(-5)
    printf("\nSuprisngli thsi also works");
    if('a')
    printf("\nHello");
}


//if(Its not condition it actual an expression)
//expression can be character number or anything like aassignment